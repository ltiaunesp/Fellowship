angular.module('fellowship.modules.missions.services')
  .service("MissionService", function(ApiService){
    var service = {};

    service.addQuests = function(title, desc, points, timeToComplete, deadline, owner){

    }

    service.listQuests = function(){
      
    }

    return service;
  });
